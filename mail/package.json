{
  "name": "mail",
  "version": "0.0.0",
  "description": "Mailbox handling",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "BSD-2-Clause",
  "dependencies": {
      // Used to wait for DB queries to complete
    "async": "~0.9.0",

      // Testing framework. Type 'mocha' to run the tests
    "mocha": "^2.2.1",

      // Database driver used by mail.sqlite.js
    "sqlite3": "~3.0.5"
  }
}
