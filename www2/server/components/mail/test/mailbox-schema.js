var schema = require('../mailbox-schema.js');
var assert = require('assert');

it(
    'Schema',
    function() {
	describe(
	    'Should fail',
	    function() {
		assert(false);
	    }
	);
    }
);
