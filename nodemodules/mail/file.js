var labels = require('./labels.js');

function packFileData(path, type, cb) {
  type = type || 'unspecified';
  
}

// Send a file from the current mailbox.
function sendFile(mailbox, dst, path) {
  
}
